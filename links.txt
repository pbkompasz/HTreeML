1. Fetch data /w curl
2. Parse HTML /w htmltidy.c https://curl.se/libcurl/c/htmltidy.html
3. Generate graph from data
4. Map website using BFS search
5. Output map like 'tree'

Misc:

	1. devdungeon.com/content/how-write-command-line-tools
	2. output as tree: ls -aR | grep ":$" | perl -pe 's/:$//;s/[^-][^\/]*\//    /g;s/^    (\S)/└── \1/;s/(^    |    (?= ))/│   /g;s/    (\S)/└── \1/'
